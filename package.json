{
  "name": "conversion-microservices",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm -r --parallel start",
    "generate-schemas": "pnpm --filter @conversion-microservices/shared generate-interfaces",
    "validate-schemas": "pnpm --filter @conversion-microservices/shared test"
  },
  "keywords": [
    "microservices",
    "conversion",
    "pdf",
    "image",
    "qrcode",
    "monorepo"
  ],
  "author": "",
  "license": "ISC",
  "description": "A web application with various file conversion microservices",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=16",
    "pnpm": ">=8"
  },
  "dependencies": {
    "@nestjs/cli": "^11.0.5",
    "@nestjs/common": "^11.0.12",
    "@nestjs/core": "^11.0.12",
    "@nestjs/platform-express": "^11.0.12",
    "reflect-metadata": "^0.2.2"
  }
}
